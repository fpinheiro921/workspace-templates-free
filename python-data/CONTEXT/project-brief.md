# Project Brief — Python Script / Data Analysis

> Fill this in before starting. Cascade reads this first.

## Project Identity
- **Name:** [Project name]
- **Purpose:** [What does this script/system do? One sentence.]
- **Output:** [What does it produce? e.g. CSV, chart, report, database records]
- **Status:** `Prototype` / `Stable` / `Production`

## Stack
- **Python version:** 3.10+
- **Key libraries:** [e.g. pandas, numpy, matplotlib, requests]
- **Environment:** [venv / conda]
- **Data source(s):** [Files, APIs, databases — list them]
- **Output destination:** [Local files / cloud / database / stdout]

## Data
- **Input format:** [e.g. CSV with columns: date, open, high, low, close, volume]
- **Input location:** [e.g. `data/raw/`]
- **Output format:** [e.g. CSV, JSON, PNG chart]
- **Output location:** [e.g. `data/output/`]

## Non-Negotiables
- [e.g. Script must be idempotent — running twice = same result]
- [e.g. Never overwrite source data]
- [e.g. Must run on Windows + Mac]

## What Cascade Should Never Do
- [e.g. Don't use global variables]
- [e.g. Don't install packages not in requirements.txt]

## Environment Variables
| Variable | Purpose |
|---|---|
| `DATA_DIR` | Root data directory |
| `API_KEY` | External API key |
