# [Project Name] — REST API

> Replace this with your API description.

**Framework:** [FastAPI / Express / other]  
**Database:** [PostgreSQL / SQLite / other]  
**Status:** [Development / Staging / Production]  
**Last updated:** YYYY-MM-DD  

---

## Getting Started

```bash
# Python/FastAPI
pip install -r requirements.txt
uvicorn main:app --reload

# Node/Express  
npm install
npm run dev
```

## Workspace Setup Checklist
- [ ] Fill in `CONTEXT/project-brief.md` (especially auth strategy)
- [ ] Update `CONTEXT/index.md` Key Facts section
- [ ] Create first feature file in `FEATURES/` for your first resource
- [ ] Add environment variables to `.env` (never commit this file)

## Folder Guide
- `CONTEXT/` — API memory, data models, auth decisions
- `FEATURES/` — One file per resource/domain (users, orders, etc.)
- `TEMP/` — Drop Postman collections and rough API specs here
- `TEMPLATES/` — Endpoint spec format
- `DOCS/` — Final API documentation
